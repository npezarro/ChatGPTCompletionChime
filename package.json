{
  "name": "chatgpt-completion-chime",
  "version": "5.4.0",
  "private": true,
  "description": "Chime on ChatGPT completion — FSM-based detection",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.1.1",
    "jsdom": "^26.1.0"
  }
}
