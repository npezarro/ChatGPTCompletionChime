{
  "name": "chatgpt-completion-chime",
  "version": "5.4.0",
  "private": true,
  "type": "module",
  "description": "Chime on ChatGPT completion — FSM-based detection",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^10.4.0",
    "jsdom": "^26.1.0",
    "vitest": "^3.1.1"
  }
}
